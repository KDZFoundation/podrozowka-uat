name: Firebase Hosting

on:
  pull_request:
    branches: [main]
  push:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: firebase-cloudrun-${{ github.ref }}
  cancel-in-progress: true

env:
  FIREBASE_PROJECT_ID: podrozowka

jobs:
  quality:
    name: Quality Gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Reject insecure Firestore rules
        run: |
          if grep -Fq 'allow read, write: if true' firestore.rules; then
            echo 'Firestore rules allow anonymous full read/write access.'
            exit 1
          fi

      - name: Type check
        run: bun run typecheck

      - name: Lint
        run: bun run lint

      - name: Tests
        run: bun run test

      - name: Build UAT frontend
        run: bun run build

  deploy:
    name: Deploy Firebase Hosting UAT
    needs: quality
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: uat
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Authenticate to Google Cloud with Workload Identity
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Build Firebase Hosting bundle
        run: |
          bun install --frozen-lockfile
          bun run build:uat

      - name: Setup Node.js for Firebase CLI
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Deploy Firebase Hosting
        run: |
          npx --yes firebase-tools@latest deploy \
            --project "$FIREBASE_PROJECT_ID" \
            --only hosting,firestore:rules,storage \
            --non-interactive
